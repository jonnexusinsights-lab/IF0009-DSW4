---
marp: true
theme: default
title: Tema 4.4 - HTML5 Semántico y CSS3 Avanzado
paginate: true
_class: lead
style: |
  section {
    background-color: #0f172a;
    color: #f8fafc;
    font-family: 'Segoe UI', system-ui, sans-serif;
  }
  h1, h2 {
    color: #38bdf8;
  }
  h3 {
    color: #7dd3fc;
  }
  footer {
    color: #64748b;
  }
  code {
    background-color: #1e293b;
    color: #38bdf8;
  }
  pre code {
    background-color: transparent;
    color: #f8fafc;
  }
---

# IF0009 - Desarrollo de Software IV

## Tema 4.4: HTML5 Semántico y CSS3 Avanzado (Flexbox, Grid y Mobile-First)

Universidad de Costa Rica <br>
Sede del Atlántico - Recinto Paraíso <br>
Carrera de Informática Empresarial <br>
**Profesor:** Mag. Jonathan Granados C. <br>
**Semestre:** II-2026

<footer>UCR - Informática Empresarial</footer>

---

# HTML5 Semántico y el DOM Tree

HTML5 estructura el significado del contenido y construye en memoria el **Árbol del DOM (Document Object Model Tree)**.

* **Etiquetas Semánticas de Estructura:**
  * `<header>`: Encabezado del documento o sección.
  * `<nav>`: Bloque de navegación principal.
  * `<main>`: Contenido principal e incremental (1 por página).
  * `<section>`: Agrupación temática con encabezado.
  * `<article>`: Composición autónoma re-distribuible.
  * `<aside>`: Barra lateral o contenido tangencial.
  * `<footer>`: Pie de página o cierre legal/contacto.

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado</footer>

---

# Formularios Accesibles en HTML5 (WCAG 2.1 AA)

La vinculación explícita mediante `<label for="id">` y atributos nativos garantiza accesibilidad universal para lectores de pantalla.

```html
<form id="frmUsuario">
    <div class="field">
        <label for="txtNombre">Nombre Completo:</label>
        <input type="text" id="txtNombre" name="nombre" 
               required minlength="3" autocomplete="name">
    </div>
    <div class="field">
        <label for="txtCorreo">Correo Electronico:</label>
        <input type="email" id="txtCorreo" name="correo" required>
    </div>
    <button type="submit">Guardar Registro</button>
</form>
```

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado</footer>

---

# El Modelo de Caja CSS (Box Model)

Cada elemento HTML se renderiza como una caja concéntrica de 4 capas: **Content**, **Padding**, **Border** y **Margin**.

```css
/* Regla Universal Obligatoria */
*, *::before, *::after {
    box-sizing: border-box;
}
```

* **Sin `border-box` (`content-box`):**
  * `Ancho Final = width + padding + border` (Provoca desbordamientos).
* **Con `border-box`:**
  * `width` incluye el `padding` y el `border` de forma elástica.

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado</footer>

---

# Especificidad CSS `(a, b, c, d)`

Determina la prioridad de los estilos cuando múltiples selectores coinciden.

* **a (1000):** Estilos inline (`style="..."`).
* **b (100):** Selectores de ID (`#txtNombre`).
* **c (10):** Clases (`.btn`), Atributos (`[type="email"]`) y Pseudo-clases (`:hover`).
* **d (1):** Elementos HTML (`h1`, `div`) y Pseudo-elementos (`::before`).

$$\text{Especificidad} = (a \cdot 1000) + (b \cdot 100) + (c \cdot 10) + d$$

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado</footer>

---

# CSS Flexbox (Diagramación Unidimensional)

Ideal para organizar elementos en un único eje (Fila o Columna).

```css
.flex-container {
    display: flex;
    flex-direction: row; /* Eje Principal: Horizontal */
    justify-content: space-between; /* Alineacion Eje Principal */
    align-items: center; /* Alineacion Eje Cruzado */
    flex-wrap: wrap; /* Permitir salto de linea */
}
```

* **Propiedades Clave:**
  * `justify-content`: `flex-start`, `center`, `space-between`.
  * `align-items`: `stretch`, `center`, `flex-end`.

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado</footer>

---

# CSS Grid Layout (Diagramación Bidimensional)

Maquetación fluida en Filas y Columnas simultáneas.

```css
.grid-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
}
```

* **`repeat(auto-fit, minmax(250px, 1fr))`:**
  * Crea una retícula elástica autoadaptable.
  * Mantiene las columnas en un tamaño mínimo de `250px`.
  * Rena y distribuye proporcionalmente (`1fr`) el espacio sobrante.

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado</footer>

---

# Variables CSS y Enfoque Mobile-First

```css
:root {
    --primary: #38bdf8;
    --bg-dark: #0f172a;
}

/* 1. Estilos Base para Dispositivos Moviles */
.layout {
    display: flex;
    flex-direction: column;
}

/* 2. Media Query Adaptativa para Escritorio */
@media (min-width: 768px) {
    .layout {
        display: grid;
        grid-template-columns: 240px 1fr;
    }
}
```

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado</footer>

---

# 🛠️ Práctica Paso a Paso: Dashboard LogiExpress

Construiremos paso a paso un **Panel de Control de Envíos Responsivo**:

* **Objetivo:** Aplicar HTML5 Semántico, CSS Variables, Flexbox, Grid y Media Queries Mobile-First.
* **Componentes del Dashboard:**
  1. Encabezado Flexbox con Logo y Perfil.
  2. Menú de Navegación Lateral (Sidebar).
  3. Retícula de Métricas con CSS Grid (`auto-fit`).
  4. Formulario de Registro Accesible.

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado - Práctica</footer>

---

# Práctica Paso 1: Estructura HTML5 Semántica

```html
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LogiExpress Dashboard</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <header class="app-header">
        <div class="logo">🚚 LogiExpress</div>
        <nav class="user-info">Admin Ops</nav>
    </header>
    <div class="dashboard-layout">
        <!-- Sidebar y Main Content -->
    </div>
</body>
</html>
```

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado - Práctica</footer>

---

# Práctica Paso 2: Reset Global y CSS Variables

```css
:root {
    --bg-main: #0b0f19;
    --bg-surface: #111827;
    --bg-card: #1e293b;
    --primary: #38bdf8;
    --text-main: #f8fafc;
    --text-muted: #94a3b8;
    --border: #334155;
    --gap: 1.25rem;
}

*, *::before, *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    font-family: system-ui, sans-serif;
    background-color: var(--bg-main);
    color: var(--text-main);
}
```

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado - Práctica</footer>

---

# Práctica Paso 3: Encabezado Flexbox

```css
.app-header {
    background-color: var(--bg-surface);
    border-bottom: 1px solid var(--border);
    padding: 1rem 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-size: 1.25rem;
    font-weight: bold;
    color: var(--primary);
}

.user-info {
    color: var(--text-muted);
    font-size: 0.9rem;
}
```

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado - Práctica</footer>

---

# Práctica Paso 4: Retícula de Métricas con CSS Grid

```html
<section class="metrics-grid">
    <article class="card-metric">
        <h3>Envíos Activos</h3>
        <p class="value">1,248</p>
    </article>
    <article class="card-metric">
        <h3>En Tránsito</h3>
        <p class="value">856</p>
    </article>
</section>
```

```css
.metrics-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--gap);
}
```

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado - Práctica</footer>

---

# Práctica Paso 5: Formulario Accesible Responsivo

```html
<form id="frmPaquete" class="form-grid">
    <div class="field">
        <label for="txtTracking">Codigo Rastreo:</label>
        <input type="text" id="txtTracking" required>
    </div>
    <div class="field">
        <label for="numPeso">Peso (kg):</label>
        <input type="number" id="numPeso" step="0.1" required>
    </div>
    <button type="submit" class="btn-primary">Guardar Paquete</button>
</form>
```

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado - Práctica</footer>

---

# Práctica Paso 6: Estilos del Formulario Grid

```css
.form-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
}

.field {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
}

.field input {
    background: var(--bg-main);
    border: 1px solid var(--border);
    color: var(--text-main);
    padding: 0.6rem;
    border-radius: 6px;
}
```

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado - Práctica</footer>

---

# Práctica Paso 7: Media Queries (Breakpoint Desktop)

```css
/* Adaptacion para Pantallas >= 768px */
@media (min-width: 768px) {
    .dashboard-layout {
        display: grid;
        grid-template-columns: 200px 1fr;
    }

    .form-grid {
        grid-template-columns: repeat(2, 1fr);
    }

    .btn-primary {
        grid-column: span 2;
        justify-self: end;
        width: auto;
    }
}
```

<footer>Tema 4.4: HTML5 Semántico y CSS3 Avanzado - Práctica</footer>

---

# Pregunta de Reflexión

En el desarrollo de una Single-Page Application (SPA) moderna:

¿Qué ventajas técnicas en Accesibilidad (WCAG 2.1 AA), rendimiento y mantenibilidad ofrece utilizar etiquetas semánticas nativas (`<button>`, `<header>`, `<nav>`) en lugar de contenedores genéricos `<div class="...">` con controladores `onclick`?

<footer>UCR - Informática Empresarial</footer>
