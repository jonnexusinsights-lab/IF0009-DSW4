---
marp: true
theme: default
title: Tema 3.3 - Capa de Controladores y OpenAPI
paginate: true
_class: lead
style: |
  section {
    background-color: #0f172a;
    color: #f8fafc;
    font-family: 'Segoe UI', system-ui, sans-serif;
  }
  h1, h2 {
    color: #38bdf8;
  }
  h3 {
    color: #7dd3fc;
  }
  footer {
    color: #64748b;
  }
  code {
    background-color: #1e293b;
    color: #38bdf8;
  }
  pre code {
    background-color: transparent;
    color: #f8fafc;
  }
---

# IF0009 - Desarrollo de Software IV

## Tema 3.3: Capa de Controladores, APIs RESTful y Documentación con OpenAPI

Universidad de Costa Rica <br>
Sede del Atlántico - Recinto Paraíso <br>
Carrera de Informática Empresarial <br>
**Profesor:** Mag. Jonathan Granados C. <br>
**Semestre:** II-2026

<footer>UCR - Informática Empresarial</footer>

---

# La Capa de Controladores (`@RestController`)

* **Definición de Entrada al Sistema:**
  * Responsable de recibir las solicitudes de red del cliente.
  * Valida formalmente la estructura y contenido de los datos (DTOs).
  * Delega la lógica de negocio a la capa de servicios.
  * Construye y retorna la respuesta de red correspondiente.
* **Componente de Spring Boot:**
  * Declarado con la anotación estereotipo `@RestController`.
  * Combina `@Controller` (manejo de endpoints) y `@ResponseBody` (retorno directo en formato JSON).

<footer>Tema 3.3: Capa de Controladores y OpenAPI</footer>

---

# Fundamentos de HTTP y Verbos de Red

* **HTTP es Stateless (Sin Estado):**
  * Cada petición-respuesta es independiente. No se guarda sesión local en RAM.
* **Verbos HTTP Principales:**
  * `GET`: Recupera recursos. Operación segura e idempotente.
  * `POST`: Registra nuevos recursos en base de datos.
  * `PUT`: Reemplaza la totalidad del recurso indicado. Idempotente.
  * `PATCH`: Modificación parcial de los campos de un recurso.
  * `DELETE`: Elimina permanentemente el recurso del servidor. Idempotente.

<footer>Tema 3.3: Capa de Controladores y OpenAPI</footer>

---

# Semántica de Códigos de Respuesta HTTP

* **2xx (Éxito):**
  * `200 OK` (Operación exitosa), `201 Created` (Creación de recurso exitosa), `204 No Content` (Éxito sin datos de retorno).
* **4xx (Error del Cliente):**
  * `400 Bad Request` (Datos inválidos o nulos).
  * `401 Unauthorized` (Autenticación ausente), `403 Forbidden` (Acceso denegado por roles).
  * `404 Not Found` (Recurso no encontrado), `409 Conflict` (Violación de unicidad).
* **5xx (Error de Servidor):**
  * `500 Internal Error` (Excepción no controlada), `503 Unavailable` (Caída de red).

<footer>Tema 3.3: Capa de Controladores y OpenAPI</footer>

---

# De Arquitectura SOA a Microservicios

* **SOA (Arquitectura Orientada a Servicios):**
  * Tradicionalmente pesada. Integra aplicaciones usando buses de servicios (ESB) y mensajes XML grandes sobre SOAP.
* **Microservicios y Desacoplamiento:**
  * Descompone la aplicación en servicios autónomos e independientes.
  * Cada microservicio gestiona su base de datos.
  * Comunicación ágil utilizando **APIs RESTful** y formato liviano **JSON**.
  * Permite desacoplar el desarrollo del backend de clientes web (Angular) o móviles.

<footer>Tema 3.3: Capa de Controladores y OpenAPI</footer>

---

# Documentación con OpenAPI y Swagger

* **Especificación OpenAPI:**
  * Estándar para describir servicios REST de forma legible para humanos y máquinas.
* **Swagger UI:**
  * Interfaz interactiva autogenerada en tiempo de ejecución.
  * Accesible en la ruta local: `/swagger-ui/index.html`.
* **Biblioteca Spring Boot:**
  * Integración directa añadiendo el starter en `pom.xml`:
  `springdoc-openapi-starter-webmvc-ui`

<footer>Tema 3.3: Capa de Controladores y OpenAPI</footer>

---

# Analogías del Tema

* **El Mesero de la Pizzería (El Controlador):**
  * Usted no cocina los ingredientes ni enciende el horno. Le pide al mesero (Controlador) su orden. El mesero valida la petición y la envía a la cocina (Capa de Negocios). Si la pizza está lista, se la entrega (`200 OK`). Si pide sushi, le dice directamente que no se puede (`400 Bad Request`).
* **El Menú Exterior (OpenAPI/Swagger):**
  * El letrero en la entrada del restaurante describe exactamente qué puede ordenar, qué ingredientes lleva y qué precio tiene, permitiéndole interactuar de forma segura sin adivinar.

<footer>Tema 3.3: Capa de Controladores y OpenAPI</footer>

---

# Código: Objeto de Transferencia (`CursoDTO.java`)

```java
@Schema(description = "Modelo para la creación de un curso")
public class CursoDTO {
    @Schema(description = "Nombre del curso", example = "Sistemas")
    @NotBlank(message = "El nombre del curso no puede estar vacío.")
    private String nombre;

    @Schema(description = "Cupos máximos", example = "30")
    @NotNull(message = "Los cupos son obligatorios.")
    @Min(value = 1, message = "Mínimo 1 cupo.")
    private Integer cuposDisponibles;

    // Getters y setters...
}
```

<footer>Tema 3.3: Capa de Controladores y OpenAPI</footer>

---

# Código: El Controlador REST y Swagger (Tag y GET)

```java
@RestController
@RequestMapping("/api/cursos")
@Tag(name = "Cursos", description = "Gestión de cursos académicos")
public class CursoRestController {
    private final MatriculaService matriculaService;

    public CursoRestController(MatriculaService matriculaService) {
        this.matriculaService = matriculaService;
    }

    @Operation(summary = "Obtener un curso por ID", description = "Busca curso.")
    @ApiResponses({
        @ApiResponse(responseCode = "200", description = "Encontrado"),
        @ApiResponse(responseCode = "404", description = "No encontrado")
    })
    @GetMapping("/{id}")
    public ResponseEntity<Curso> obtenerPorId(@PathVariable Integer id) {
        // ... Lógica ...
    }
}
```

<footer>Tema 3.3: Capa de Controladores y OpenAPI</footer>

---

# Código: Método POST y Validación DTO

```java
    @Operation(summary = "Crear curso", description = "Guarda curso validado.")
    @ApiResponses({
        @ApiResponse(responseCode = "201", description = "Curso creado"),
        @ApiResponse(responseCode = "400", description = "Datos inválidos")
    })
    @PostMapping
    public ResponseEntity<Curso> crear(
            @Validated @RequestBody CursoDTO dto) {
        Curso nuevoCurso = new Curso(dto.getNombre(), dto.getCuposDisponibles());
        nuevoCurso.setId(10); // Simulación
        return ResponseEntity.status(HttpStatus.CREATED).body(nuevoCurso);
    }
```

<footer>Tema 3.3: Capa de Controladores y OpenAPI</footer>

---

# Trampas Comunes en Controladores

* **Ignorar Códigos HTTP (Retornar siempre 200 OK):**
  * Retornar un body de error con código HTTP 200. Esto confunde a los clientes de red (Angular) e impide procesar fallas por interceptores.
* **Exposición Directa de Entidades JPA:**
  * Provoca bucles de mapeo JSON infinito si hay relaciones `@ManyToMany` o `@OneToMany`.
* **Ignorar Validación Automática:**
  * Omitir `@Validated` o `@Valid` en el `@RequestBody`, ingresando registros con campos inválidos o nulos.

<footer>Tema 3.3: Capa de Controladores y OpenAPI</footer>

---

# Pregunta de Reflexión

* Dado que el diseño REST es sin estado (*stateless*), el servidor de aplicaciones no almacena información de login de los usuarios en la memoria RAM:
  * ¿Cómo contribuye esta propiedad del protocolo a la tolerancia a fallas y la escalabilidad horizontal en entornos de la nube con microservicios?
  * ¿Qué metadatos en la petición HTTP reemplazan la necesidad de mantener el estado de sesión en el servidor?

<footer>Tema 3.3: Capa de Controladores y OpenAPI</footer>
